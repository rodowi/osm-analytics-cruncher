{
  "name": "osm-analytics-server",
  "version": "0.0.0",
  "description": "serves vector tiles for osm-analytics",
  "main": "serve.js",
  "dependencies": {
    "express": "^4.13.4",
    "mbtiles": "^0.8.2",
    "tilelive": "^5.12.1"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
